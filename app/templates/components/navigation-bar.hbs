<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
          {{#link-to 'blogs' class="navbar-brand"}}Blog{{/link-to}}

        </div>

        <div class="collapse navbar-collapse" id="main-navbar">
            <ul class="nav navbar-nav">
              {{#link-to 'index' tagName="li"}}<a href="">Invitation</a>{{/link-to}}
            </ul>

            <ul class="nav navbar-nav navbar-right">
              {{#if loginService.isAuthenticated}}
                  <p class="navbar-text">{{loginService.currentUser.email}}</p>
                  <li><a {{action 'logout'}} href="#">Logout</a></li>
              {{else}}
                {{#link-to 'login' tagName="li"}}<a href="">Login</a>{{/link-to}}
                {{#link-to 'users.new' tagName="li"}}<a href="">SignUp</a>{{/link-to}}
              {{/if}}
            </ul>

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>